# ASKMYDOCS-RAG-System-for-Document-Intelligence

ASKMYDOCS is a Retrieval-Augmented Generation (RAG) system that allows users to ask questions over private PDF documents.
The system retrieves relevant document sections using semantic search with vector embeddings and generates answers using a Large Language Model (LLM).
This approach improves answer accuracy and reduces hallucinations without requiring model fine-tuning.
